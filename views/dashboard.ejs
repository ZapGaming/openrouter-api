<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Zandymon | Digital World</title>
    <style>
        :root { --bg: #0b0e14; --card: #151921; --accent: #7289da; --text: #dcddde; }
        body { background: var(--bg); color: var(--text); font-family: 'Inter', sans-serif; margin: 0; }
        .nav { background: var(--card); padding: 20px; display: flex; justify-content: space-between; border-bottom: 2px solid var(--accent); }
        .arena-container { padding: 40px; display: flex; flex-direction: column; align-items: center; }
        .monster-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(320px, 1fr)); gap: 25px; width: 90%; }
        .card { background: var(--card); border-radius: 12px; padding: 20px; border: 1px solid #333; transition: 0.3s; position: relative; }
        .card:hover { border-color: var(--accent); box-shadow: 0 0 20px rgba(114, 137, 218, 0.2); }
        .stat-row { display: flex; justify-content: space-between; margin: 5px 0; font-size: 0.9rem; }
        .battle-btn { width: 100%; background: var(--accent); color: white; border: none; padding: 12px; border-radius: 6px; cursor: pointer; font-weight: bold; margin-top: 15px; }
        #battle-modal { display: none; position: fixed; top: 50%; left: 50%; transform: translate(-50%, -50%); width: 80%; height: 80%; background: #000; border: 2px solid var(--accent); z-index: 100; padding: 20px; overflow-y: auto; }
        .log-entry { font-family: monospace; color: #a6e3a1; border-bottom: 1px solid #222; padding: 5px; }
    </style>
</head>
<body>
    <div class="nav">
        <div>Rank: <b><%= user.rank %></b> | Essence: 💰<b><%= user.essence %></b></div>
        <div>Record: 🏆 <%= user.records.wins %>W - 💀 <%= user.records.losses %>L</div>
    </div>

    <div class="arena-container">
        <h1>COMMAND CENTER</h1>
        <div class="monster-grid">
            <% user.monsters.forEach(m => { %>
                <div class="card">
                    <span style="font-size: 2.5rem;"><%= m.emoji %></span>
                    <h2><%= m.name %> <small>[Lvl <%= m.level %>]</small></h2>
                    <p style="color: var(--accent); font-style: italic;"><%= m.elDescription %></p>
                    <div class="stat-row"><span>⚔️ Attack</span> <span><%= m.atk %></span></div>
                    <div class="stat-row"><span>🛡️ Defense</span> <span><%= m.def %></span></div>
                    <div class="stat-row"><span>⚡ Speed</span> <span><%= m.spd %></span></div>
                    <div class="stat-row"><span>❤️ HP</span> <span><%= m.hp %>/<%= m.maxHp %></span></div>
                    <button class="battle-btn" onclick="startBattle('<%= m._id %>')">ENTER ARENA</button>
                </div>
            <% }) %>
        </div>
    </div>

    <div id="battle-modal">
        <h2 id="battle-title">INITIALIZING COMBAT...</h2>
        <div id="battle-logs"></div>
        <button class="battle-btn" onclick="location.reload()">CLOSE PORTAL</button>
    </div>

    <script>
        async function startBattle(mId) {
            document.getElementById('battle-modal').style.display = 'block';
            const res = await fetch('/duel-ai', {
                method: 'POST',
                headers: {'Content-Type': 'application/json'},
                body: JSON.stringify({ token: "<%= token %>", monsterId: mId })
            });
            const data = await res.json();
            const logDiv = document.getElementById('battle-logs');
            logDiv.innerHTML = `<h3>Enemy: ${data.enemy.name} (${data.enemy.element})</h3>`;
            
            data.log.forEach((line, i) => {
                setTimeout(() => {
                    const p = document.createElement('div');
                    p.className = 'log-entry';
                    p.innerText = `> ${line}`;
                    logDiv.appendChild(p);
                }, i * 500);
            });
        }
    </script>
</body>
</html>
